import {SignModalDirective} from './sign-modal.directive';

describe('SignModalDirective', () => {
  it('should create an instance', () => {
    // const directive = new SignModalDirective();
    // expect(directive).toBeTruthy();
  });
});
